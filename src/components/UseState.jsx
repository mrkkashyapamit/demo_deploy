import React from 'react';

function UseState({name}) {
    console.log(name);
    return (
        <div>
            <h1>Hi! , {name} How Are you!</h1>
        </div>
    );
}

export default UseState;